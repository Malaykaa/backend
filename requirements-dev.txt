# Dépendances de développement / tests
# Installer avec : pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.3.0
pytest-asyncio>=0.24.0
ruff>=0.8.0
black>=24.10.0
mypy>=1.13.0

# Tests d'intégration
testcontainers[postgres]>=4.8.0
