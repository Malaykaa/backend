# Généré depuis pyproject.toml — installer avec : pip install -r requirements.txt
# Pour les dépendances de dev/tests : pip install -r requirements-dev.txt
# fastapi 0.120+ requis pour supporter starlette 0.49 (CVE-2025-62727, CVE-2025-54121)
fastapi>=0.120.4,<0.121
# Pin explicite des deps transitives vulnérables (déterminisme Docker)
starlette>=0.49.1,<0.50
urllib3>=2.6.3,<3
# cryptography 46.0.6 corrige CVE-2026-26007 (validation clé pub) et CVE-2026-34073 (DNS name constraints TLS)
cryptography>=46.0.6,<47
# requests 2.33.0 corrige CVE-2026-25645 (filename prédictible dans extract_zipped_paths)
requests>=2.33.0,<3
uvicorn[standard]>=0.34.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
SQLAlchemy>=2.0.36
alembic>=1.14.0
psycopg2-binary>=2.9.10
# asyncpg — driver async pour SQLAlchemy AsyncSession (routers SSE critiques)
# psycopg2-binary reste pour Alembic et APScheduler (sync)
asyncpg>=0.30.0,<1
# pgvector : type Vector côté ORM + binding psycopg2 pour la recherche sémantique
pgvector>=0.3.6,<0.4
python-jose[cryptography]>=3.3.0
# bcrypt utilisé directement dans app/core/security.py (sans passlib)
bcrypt>=4.2.0
# pywebpush 2.4+ exige cryptography>=47, incompatible avec le pin CVE ci-dessus
# (cryptography<47) — 2.3.0 est la dernière version compatible.
pywebpush==2.3.0
openai>=1.58.0,<2
google-genai>=1.0.0
anthropic>=0.95.0
# python-multipart 0.0.26 corrige CVE-2026-40347 (DoS multipart) et CVE-2026-24486 (path traversal)
python-multipart>=0.0.26
httpx>=0.28.1
structlog>=24.4.0
email-validator>=2.1.0
apscheduler>=3.10.0
apify-client>=2.5.0
redis>=5.2.0
twilio>=9.4.0
slowapi>=0.1.9
# PDF export — utilisé par DocumentService.export_pdf() via /documents/{id}/export
reportlab>=4.2.0,<5
# PDF text extraction — utilisé pour lire les PDFs uploadés dans le chat
pypdf>=4.0.0,<5
